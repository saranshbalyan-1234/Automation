import React from "react";
import { connect } from "react-redux";

export const ManagerUserProject = (props) => {
  return <div>ManagerUserProject</div>;
};

const mapStateToProps = (state) => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(ManagerUserProject);
